<h1><%= @lair.name %></h1>
<p><%= @lair.price %>€</p>
<p><%= @lair.description %></p>

<h2>Make a reservation</h2>
<%= simple_form_for([@lair, @reservation]) do |f| %>
  <%# <%= f.input :start_date, as: :date, html5: true %> 
  <%= f.input :end_date, as: :date, html5: true %>
  <%= f.input :start_date,
            as: :string,
            input_html: { data: { controller: "datepicker" } } %>
  <%= f.submit %>
<% end %>
